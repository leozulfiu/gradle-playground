apply plugin: "java-gradle-plugin"

gradlePlugin {
    plugins {
        java {
            id = 'deployment'
            implementationClass = 'com.leo.deployment.DeploymentPlugin'
        }
    }
}
