plugins {
    id 'java-gradle-plugin'
}

subprojects {
    apply plugin: 'groovy'

    repositories {
        mavenCentral()
    }

    dependencies {
        compile gradleApi()
        compile localGroovy()
    }
}

dependencies {
    runtime subprojects.collect { project(it.path) }
}
